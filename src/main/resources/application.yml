app:
  mongodb:
    uri: ${MONGODB_URI}
    keyVaultNamespace: encryption.__keyVault
    encryptedDatabaseName: hrsystem
    encryptedCollectionName: employee
    cryptSharedLibPath: ${CRYPT_PATH}
logging:
  level:
    org.springframework.data.mongodb: DEBUG